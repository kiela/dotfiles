#!/usr/bin/env bash

set -euo pipefail

# Idea taken from:
# https://stackoverflow.com/a/46726373

# Check to see if a pipe exists on stdin.
if [[ -p /dev/stdin ]]; then
  tr '[:upper:]' '[:lower:]'
elif [[ $# -gt 0 ]]; then
  printf '%s\n' "$*" | tr '[:upper:]' '[:lower:]'
fi
