#!/usr/bin/env bash

set -euo pipefail

# Idea taken from:
# https://stackoverflow.com/a/46726373

# Check to see if a pipe exists on stdin.
if [[ -p /dev/stdin ]]; then
  tr '[:lower:]' '[:upper:]'
elif [[ $# -gt 0 ]]; then
  printf '%s\n' "$*" | tr '[:lower:]' '[:upper:]'
fi
